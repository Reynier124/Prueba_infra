apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matomo
spec:
  rules:
    - host: reynier124-kb.my.kube.um.edu.ar
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: matomo
                port:
                  number: 80
